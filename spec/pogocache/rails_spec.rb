# frozen_string_literal: true

RSpec.describe Pogocache::Rails do
  it "has a version number" do
    expect(Pogocache::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
